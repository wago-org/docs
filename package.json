{
  "name": "@wago/docs",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.7.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "docs:prepare": "node scripts/generate-discovery.mjs",
    "docs:dev": "npm run docs:prepare && vitepress dev docs",
    "docs:build": "npm run docs:prepare && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:verify": "node scripts/verify-build.mjs",
    "docs:check": "npm run docs:build && npm run docs:verify"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
